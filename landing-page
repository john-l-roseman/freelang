Create a single HTML file called index.html with the following specs.

Page Layout:
Left side header: openlang.ai  (large bold, link to "/")
Middle header: LANGUAGE Language Matching Games
Put a button with the text “Source” in the upper right corner, with the URL 
(TXT file already exists at that location).

Create a dataset of Categories: "Quote Match","Word Match","Phrase Match"
Add a field to the dataset with an SVG graphic clip art representation of the Category.

Create a dataset of Games with a field NAME.
Populate the NAME field with these games: ALL_GAMES.

Create three blocks, one for each category.
Each block contains a header, with the Category name and SVG image.

Under the header, display a grid of Game buttons that contain NAME. Link each button to this URL: “/LANGUAGE/NAME” 
The Game Buttons are distributed in the categories in this way: 
Quote Match: QUOTE_GAMES
Word Match: WORD_GAMES
Phrase Match: PHRASE_GAMES


STRICT: Do not add any notes at the top of the index.html file.


